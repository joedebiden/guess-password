#include <stdio.h>

int main() {
    printf("Devine le password\n");
    //use other function
}

int password(){
    password = [azerty12];
}

int element_commun(){
    scanf(element) = user_pass
    create user_pass[]
    cmp user_pass[] to password[]
    //print only the same caracter in the right order from both list
}

int compare(){
    boolean guessed = false
    while true:
        if : user_pass == password:
            printf("bravo tu as trouvé le mot de passe\n")
            return guessed = true
        else :
            printf(element_commun)
            printf("cherche encore\n");

}